apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: enterprise-demo
spec:
  selector:
    matchLabels:
      app: enterprise-demo
  endpoints:
  - port: http
    path: /ready
    interval: 30s  # Adjust as needed
  namespaceSelector:
    matchNames:
    - dev
    - staging